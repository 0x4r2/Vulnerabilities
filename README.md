# Vulnerabilities

Open resource for practicing vulnerabilities. Resources and vulnerable apps intentionally created for learning.

Each vulnerable application lives in its own top-level directory (e.g. `LFI-PathTraversal/`). Every directory has its own `README.md` with setup and exploitation instructions — check that README before running anything.
